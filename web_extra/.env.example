# Demo database credentials. Copy to /web/.env on the target host.
# provision.sh generates this automatically with a random password.
# Never commit the real .env - it is gitignored and apache denies dotfiles.
DB_HOST=localhost
DB_USER=signcollect
DB_PASS=changeme
DB_NAME=admin_gebarenoverleg
